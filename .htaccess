<IfModule mod_rewrite.c>
    RewriteEngine On
    # Serve static assets directly from the public folder
    RewriteRule ^(assets/.*)$ public/$1 [L]

    # Route all requests to public/index.php
    RewriteRule ^(.*)$ public/index.php [L]
</IfModule>
