<IfModule mod_rewrite.c>
    RewriteEngine On

    # Stop processing if a physical file or directory exists
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

    # Otherwise route to index.php
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>
